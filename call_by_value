#include <stdio.h>
#include <stdlib.h>

// Call by value

void degerdegistir(int a,int b){
	int gecici = a;
	
	a = b;
	
	b = gecici;
	
	printf("a = %d b = %d\n",a,b);
	 
}

int main(int argc, char *argv[]) {
	
int x = 5,y = 10;

degerdegistir(x,y);

printf("x = %d y = %d",x,y);
	
	
	return 0;
}
